ARG VLLM_ASCEND_IMAGE=quay.io/atlas-ci/vllm-ascend
ARG VLLM_ASCEND_TAG=v0.28.0
ARG VLLM_ASCEND_BASE=${VLLM_ASCEND_IMAGE}:${VLLM_ASCEND_TAG}
FROM ${VLLM_ASCEND_BASE}

SHELL ["/bin/bash", "-c"]

ARG APP_DIR=/vllm-workspace/vllm-omni
# Source of the vllm-omni repo to clone into the image (instead of COPY .).
ARG VLLM_OMNI_REPO=https://github.com/vllm-project/vllm-omni.git
ARG VLLM_OMNI_REF=main
# Optional full SHA for a reproducible nightly build.
ARG VLLM_OMNI_COMMIT=
LABEL org.opencontainers.image.revision=${VLLM_OMNI_COMMIT}
# Temporary workaround for the empty Triton namespace directories left in the
# vllm-ascend 310P amd64 image after uninstalling triton and triton-ascend.
ARG PATCH_310P_TRITON_RESIDUE=0

RUN if [[ "${PATCH_310P_TRITON_RESIDUE}" == "1" ]]; then \
        python3 -c 'import shutil, site; from pathlib import Path; [shutil.rmtree(Path(root) / "triton", ignore_errors=True) for root in site.getsitepackages()]' && \
        python3 -c 'import importlib.util; assert importlib.util.find_spec("triton") is None'; \
    fi

RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        git \
        ffmpeg \
        libgl1 \
        libglib2.0-0 \
        libgomp1 \
        libsm6 \
        libxext6 \
        libxrender1 \
        libxcb1 \
        espeak-ng && \
    rm -rf /var/cache/apt/* && \
    rm -rf /var/lib/apt/lists/*

RUN set -euo pipefail; \
    if [[ -n "${VLLM_OMNI_COMMIT}" ]]; then \
        [[ "${VLLM_OMNI_COMMIT}" =~ ^[0-9a-f]{40}$ ]]; \
        git init "${APP_DIR}"; \
        git -C "${APP_DIR}" remote add origin "${VLLM_OMNI_REPO}"; \
        git -C "${APP_DIR}" fetch --depth 1 origin "${VLLM_OMNI_COMMIT}"; \
        git -C "${APP_DIR}" checkout --detach FETCH_HEAD; \
        test "$(git -C "${APP_DIR}" rev-parse HEAD)" = "${VLLM_OMNI_COMMIT}"; \
    else \
        git clone --depth 1 --branch "${VLLM_OMNI_REF}" "${VLLM_OMNI_REPO}" "${APP_DIR}"; \
    fi
WORKDIR ${APP_DIR}

RUN source /usr/local/Ascend/ascend-toolkit/set_env.sh && \
    source /usr/local/Ascend/nnal/atb/set_env.sh && \
    export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/Ascend/ascend-toolkit/latest/`uname -i`-linux/devlib && \
    if [[ "${PATCH_310P_TRITON_RESIDUE}" == "1" ]]; then python3 -c 'import torch'; fi && \
    python3 -m pip install -U pip && \
    python3 -m pip install -v \
      "pytest>=7.0.0" \
      "pytest-asyncio>=0.21.0" \
      "pytest-cov>=4.0.0" \
      "pytest-mock>=3.10.0" \
      "openpyxl>=3.0.0" \
      "psutil>=7.2.0" \
      "soundfile>=0.13.1" \
      "pyttsx3>=2.99" \
      "opencc==1.4.1" \
      "mindiesd==3.1.0" \
      "opencv-python>=4.12.0.88" \
      "imageio[ffmpeg]>=0.6.0" \
      "jiwer>=3.0.0" \
      "zhon>=2.0.0" \
      "zhconv>=1.4.2" \
      "funasr>=1.0.0" \
      "hyperpyyaml>=1.2.0" \
      "onnx>=1.16.0" \
      "librosa>=0.10.0" \
      "datasets>=3.0.0" \
      --default-timeout=60 && \
    python3 -m pip install -v -e /vllm-workspace/vllm-omni/ --no-build-isolation \
      --default-timeout=60 && \
    python3 -m pip install -v --no-deps \
      "s3tokenizer>=0.3.0" \
      "step-audio2>=1.0.0" \
      --default-timeout=60

ENV VLLM_WORKER_MULTIPROC_METHOD=spawn

ENTRYPOINT []
